pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // ✅ Start.io Repository (Sahi URL)
        maven {
            url "https://maven.start.io/repository/maven/"
        }
    }
}

rootProject.name = "My-Apk"
include ':app'
